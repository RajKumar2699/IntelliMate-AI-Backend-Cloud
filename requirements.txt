annotated-doc==0.0.4
annotated-types==0.7.0
anyio==4.14.1
certifi==2026.6.17
cffi==2.0.0
charset-normalizer==3.4.7
click==8.4.2
cryptography==49.0.0
distro==1.9.0
fastapi==0.139.0
google-auth==2.55.1
google-genai==2.10.0
h11==0.16.0
httpcore==1.0.9
httpx==0.28.1
idna==3.18
pyasn1==0.6.3
pyasn1_modules==0.4.2
pycparser==3.0
pydantic==2.13.4
pydantic_core==2.46.4
python-dotenv==1.2.2
requests==2.34.2
sniffio==1.3.1
starlette==1.3.1
tenacity==9.1.4
typing-inspection==0.4.2
typing_extensions==4.16.0
urllib3==2.7.0
uvicorn==0.49.0
websockets==16.0

# --- Missing below: imported by app/services/*.py but not pinned above.
# pip install would have failed on a clean machine/container without these. ---
openai==1.109.1
numpy==2.1.3
soundfile==0.12.1
speechbrain==1.0.3

# CPU-only torch build. Installing plain "torch" pulls the ~700MB CUDA build,
# which is unnecessary (and often too big for free/small hosting tiers) since
# there's no GPU on Render/Railway free-cpu instances. Keep this extra index
# line ABOVE the torch pin, or pass it as --extra-index-url in your build step.
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.5.1

# NOTE: faster-whisper is imported in app/services/stt_service.py but that
# module is never actually used anywhere else in the codebase (STTService is
# dead code — Sarvam STT is what's wired in). Recommended: delete
# app/services/stt_service.py and skip this dependency entirely to keep the
# build smaller/faster. If you want to keep it for later, uncomment:
# faster-whisper==1.0.3
